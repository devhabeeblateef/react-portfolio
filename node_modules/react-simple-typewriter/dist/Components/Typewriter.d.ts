/// <reference types="react" />
import { TypewriterProps } from '../Hooks/useTypewriter';
interface ComponentProps extends TypewriterProps {
    /** Show / Hide the cursor */
    cursor?: boolean;
    /** Change the cursor style available if cursor is enabled */
    cursorStyle?: string;
    /** Change the cursor Color */
    cursorColor?: string;
}
/**
 * A Simple React Component for adding a nice Typewriter effect to your project.
 */
export declare const Typewriter: ({ words, loop, typeSpeed, deleteSpeed, delaySpeed, cursor, cursorStyle, cursorColor, onLoopDone, onType }: ComponentProps) => JSX.Element;
export {};
