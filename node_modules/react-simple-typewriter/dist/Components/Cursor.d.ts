/// <reference types="react" />
interface CursorProps {
    cursorStyle?: string;
    cursorColor?: string;
}
export declare const Cursor: ({ cursorStyle, cursorColor }: CursorProps) => JSX.Element;
export {};
